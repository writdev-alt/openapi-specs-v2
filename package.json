{
  "name": "openapi-specs",
  "version": "1.0.0",
  "description": "WRPay API OpenAPI Specification",
  "scripts": {
    "bundle": "redocly bundle openapi.yaml -o bundled.yaml --force",
    "lint": "redocly lint openapi.yaml --extends recommended",
    "preview": "redocly preview-docs openapi.yaml",
    "build": "redocly build-docs openapi.yaml -o dist/index.html"
  },
  "devDependencies": {
    "@redocly/cli": "^2.12.0",
    "js-yaml": "^4.1.1",
    "sharp": "^0.33.0"
  }
}
